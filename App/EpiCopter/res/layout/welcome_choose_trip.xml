<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_gravity="center_horizontal|center_vertical"
    android:background="@color/background"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/textView1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|center_vertical"
        android:layout_marginBottom="20dip"
        android:layout_marginTop="20dip"
        android:text="@string/my_trips" />

    <Button
        android:id="@+id/welcomeChooseTripButtonNewTrip"
        style="@style/button_text"
        android:background="@drawable/blue_button"
        android:text="@string/new_" />

    <Button
        android:id="@+id/welcomeChooseTripButtonHistoricTrip"
        style="@style/button_text"
        android:background="@drawable/blue_button"
        android:text="@string/historic" />

    <Button
        android:id="@+id/welcomeChooseTripButtonResumeTrip"
        style="@style/button_text"
        android:background="@drawable/blue_button"
        android:text="@string/resume" />

    <Button
        android:id="@+id/welcomeChooseTripButtonLogout"
        style="@style/button_text"
        android:background="@drawable/blue_button"
        android:text="@string/logout" />

</LinearLayout>